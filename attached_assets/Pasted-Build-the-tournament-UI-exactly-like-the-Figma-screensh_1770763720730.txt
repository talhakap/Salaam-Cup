Build the tournament UI exactly like the Figma screenshots I attached (Tournaments page, Tournament template, Schedule, Standings, Rules, Team page). Do not change backend logic or database tables. Use the existing tables and API calls for tournaments, divisions, teams, players, matches, standings, venues, sponsors. The tournaments page is a directory where users pick a tournament. When an admin adds a tournament in the admin panel, the public site should automatically show that tournament and its pages using a dynamic route based on tournament slug, not hardcoded pages. Create these public routes driven by database data: /tournaments (list), /tournaments/:slug (tournament landing template), /tournaments/:slug/standings, /tournaments/:slug/schedule, /tournaments/:slug/rules. Inside each of those pages, show division tabs (Pool A, Pool B, Pool C, Junior A, Masters etc) pulled from the divisions table for that tournament, and switching tabs filters the data on the page. Standings page shows standings rows for the selected division. Schedule page shows matches for the selected division with filters like date and status, and uses venue info. Rules page shows rules content for the selected division, render from stored text fields or placeholder if not available. Tournament landing page should match the template and include hero image, sponsors strip, a preview of schedule and standings, and CTA section and FAQ section like the screenshot. Team pages: when a user clicks a team anywhere (standings table team name, schedule matchup, teams list), route to /teams/:teamId (or /tournaments/:slug/teams/:teamId if that is how routing is set) and display the team header, logo, division, captain, and tabs for roster and schedule like the screenshot. Roster tab shows players on that team with jersey number, name, dob, and status. Admin requirements: add the ability in admin UI to add divisions to a tournament and edit division fields (name, category, description, game_format, registration_fee). The public division tabs should update automatically when admin adds or edits divisions. Make the UI clean and consistent with the screenshots: black and white theme, bold headings, rounded pill tabs, card lists for games, table for standings, sponsor logo strip, and a consistent footer. Build this in steps: 1) implement routes and layout shell, 2) tournaments list page, 3) tournament template page, 4) standings page with division tabs, 5) schedule page with division tabs and filters, 6) rules page with division tabs, 7) team page with roster and schedule tabs, 8) final styling pass to match the screenshots exactly.