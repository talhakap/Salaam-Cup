node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment — never commit secrets
.env
.env.local
.env.*.local

# User-uploaded files — keep the folder but ignore contents
attached_assets/uploads/**
!attached_assets/uploads/.gitkeep